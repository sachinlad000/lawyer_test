---
/**
 * Header Component
 * ================
 * Responsive header with transparent → solid navbar on scroll.
 * Navbar overlays the hero section (transparent) and becomes solid on scroll.
 *
 * CONFIGURATION: Edit src/config/site.config.ts for navigation and contact info.
 */

import { Scale, Phone, Menu, X, ChevronDown } from "@lucide/astro";
import { siteConfig } from "../config/site.config";

// Navigation comes from central config
const navItems = siteConfig.navigation.main;

// Contact info from central config
const phone = siteConfig.contact.phone.display;
const phoneClean = siteConfig.contact.phone.primary.replace(/[^0-9+]/g, "");

// Branding from config
const advocateName = siteConfig.advocate.name;
const firmTagline = "(Kulkarni)";
const barInfo = `Bar Council Registered | ${siteConfig.advocate.experience} Years of Experience`;

interface Props {
  hasHero?: boolean;
}
const { hasHero = false } = Astro.props;
---

<!-- Header Top Bar — scrolls away naturally, not fixed --><!-- <div class="header-top">
  <div class="container">
    <div class="header-top-content">
      <span class="header-top-text">
        <Scale size={16} />
        {barInfo}
      </span>
      <a href={`tel:${phoneClean}`} class="header-top-phone">
        <Phone size={16} />
        {phone}
      </a>
    </div>
  </div>
</div> --><!-- Fixed Navbar — transparent over hero, solid on scroll -->
<header class={`header ${hasHero ? "header--transparent" : ""}`} id="siteHeader">
  <nav class="navbar">
    <div class="container">
      <div class="navbar-content">
        <!-- Logo -->
        <a href="/" class="navbar-logo">
          <Scale size={36} class="logo-icon" />
          <div class="logo-text">
            <span class="logo-name">{advocateName}</span>
            <span class="logo-tagline">{firmTagline}</span>
          </div>
        </a>

        <!-- Desktop Navigation -->
        <ul class="nav-menu hide-mobile">
          {
            navItems.map((item) => (
              <li class={item.children ? "nav-item has-dropdown" : "nav-item"}>
                <a href={item.href} class="nav-link">
                  {item.label}
                  {item.children && (
                    <ChevronDown size={16} class="dropdown-icon" />
                  )}
                </a>
                {item.children && (
                  <ul class="dropdown-menu">
                    {item.children.map((subItem) => (
                      <li>
                        <a href={subItem.href} class="dropdown-link">
                          {subItem.label}
                        </a>
                      </li>
                    ))}
                  </ul>
                )}
              </li>
            ))
          }
        </ul>

        <!-- CTA Button (Desktop) -->
        <!-- <a href="/contact" class="btn btn-accent hide-mobile">
          Book Appointment
        </a> -->

        <!-- Theme Toggle Moved to Floating Component -->

        <!-- Mobile Menu Toggle -->
        <button
          class="mobile-menu-toggle hide-desktop"
          aria-label="Toggle menu"
          aria-expanded="false"
        >
          <Menu size={28} class="menu-icon" />
          <X size={28} class="close-icon" />
        </button>
      </div>
    </div>

    <!-- Mobile Navigation -->
    <div class="mobile-nav hide-desktop">
      <div class="container">
        <ul class="mobile-nav-menu">
          {
            navItems.map((item) => (
              <li class="mobile-nav-item">
                <a href={item.href} class="mobile-nav-link">
                  {item.label}
                </a>
                {item.children && (
                  <ul class="mobile-dropdown">
                    {item.children.map((subItem) => (
                      <li>
                        <a href={subItem.href} class="mobile-dropdown-link">
                          {subItem.label}
                        </a>
                      </li>
                    ))}
                  </ul>
                )}
              </li>
            ))
          }
        </ul>
        <button class="theme-toggle-mobile" id="themeToggleMobile" aria-label="Toggle theme">
          <svg class="theme-icon-sun" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <circle cx="12" cy="12" r="5"/>
            <line x1="12" y1="1" x2="12" y2="3"/>
            <line x1="12" y1="21" x2="12" y2="23"/>
            <line x1="4.22" y1="4.22" x2="5.64" y2="5.64"/>
            <line x1="18.36" y1="18.36" x2="19.78" y2="19.78"/>
            <line x1="1" y1="12" x2="3" y2="12"/>
            <line x1="21" y1="12" x2="23" y2="12"/>
            <line x1="4.22" y1="19.78" x2="5.64" y2="18.36"/>
            <line x1="18.36" y1="5.64" x2="19.78" y2="4.22"/>
          </svg>
          <svg class="theme-icon-moon" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <path d="M21 12.79A9 9 0 1111.21 3 7 7 0 0021 12.79z"/>
          </svg>
          <span>Toggle Theme</span>
        </button>
        <a href="/contact" class="btn btn-accent btn-lg mobile-cta">
          Book Appointment
        </a>
      </div>
    </div>
  </nav>
</header>

<style>
  /* ============================================
     HEADER TOP BAR — scrolls naturally
     ============================================ */
  .header-top {
    background: var(--color-primary-900);
    color: white;
    padding: 0.5rem 0;
    font-size: 0.875rem;
    position: relative;
    z-index: 101;
  }

  .header-top-content {
    display: flex;
    justify-content: space-between;
    align-items: center;
    flex-wrap: wrap;
    gap: 0.5rem;
  }

  .header-top-text {
    display: flex;
    align-items: center;
    gap: 0.5rem;
  }

  .header-top-phone {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    color: var(--color-accent-500);
    font-weight: 600;
  }

  .header-top-phone:hover {
    color: var(--color-accent-400);
  }

  /* ============================================
     FIXED NAVBAR — transparent/solid states
     ============================================ */
  .header {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 100;
    transition:
      background-color 0.35s ease,
      box-shadow 0.35s ease,
      backdrop-filter 0.35s ease;
    will-change: background-color, box-shadow;
  }

  .navbar {
    padding: 1rem 0;
    transition: padding 0.35s ease;
  }

  .navbar-content {
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  /* ---- Transparent State (over dark hero by default) ---- */
  .header--transparent {
    background: transparent;
    box-shadow: none;
  }

  .header--transparent .navbar-logo {
    color: white;
  }

  .header--transparent .logo-icon {
    color: #d3a614;
  }

  .header--transparent .logo-tagline {
    color: rgba(255, 255, 255, 0.8);
  }

  .header--transparent .nav-link {
    color: white;
  }

  .header--transparent .nav-link:hover {
    color: #d3a614;
  }

  .header--transparent .nav-link::after {
    background: #d3a614;
  }

  .header--transparent .navbar-cta {
    background: var(--color-accent-500);
    color: var(--color-primary-900);
    border-color: var(--color-accent-500);
  }

  .header--transparent .navbar-cta:hover {
    background: var(--color-accent-400);
    color: var(--color-primary-900);
    transform: translateY(-2px);
    box-shadow: 0 6px 20px rgba(212, 175, 55, 0.35);
  }

  .header--transparent .mobile-menu-toggle {
    color: white;
  }

  .header--transparent .dropdown-menu {
    background: rgba(10, 36, 99, 0.95);
    backdrop-filter: blur(12px);
    border: 1px solid rgba(255, 255, 255, 0.1);
  }

  .header--transparent .dropdown-link {
    color: rgba(255, 255, 255, 0.85);
  }

  .header--transparent .dropdown-link:hover {
    background: rgba(255, 255, 255, 0.1);
    color: white;
  }

  /* ---- Transparent State (over LIGHT hero) ---- */
  .header--transparent.header--light-bg .navbar-logo {
    color: var(--color-primary-900);
  }

  .header--transparent.header--light-bg .logo-tagline {
    color: var(--color-neutral-600);
  }

  .header--transparent.header--light-bg .nav-link {
    color: var(--color-primary-900);
  }

  .header--transparent.header--light-bg .nav-link:hover {
    color: var(--color-accent-600);
  }

  .header--transparent.header--light-bg .mobile-menu-toggle {
    color: var(--color-primary-900);
  }

  /* ---- Solid State (scrolled past hero - white background) ---- */
  .header:not(.header--transparent) {
    background: var(--surface-header-solid);
    box-shadow: 0 2px 20px rgba(0, 0, 0, 0.08);
    backdrop-filter: blur(12px);
    -webkit-backdrop-filter: blur(12px);
  }

  .header:not(.header--transparent) .navbar {
    padding: 0.75rem 0;
  }

  .header:not(.header--transparent) .navbar-logo {
    color: var(--text-heading);
  }

  .header:not(.header--transparent) .logo-icon {
    color: #d3a614;
  }

  .header:not(.header--transparent) .logo-tagline {
    color: var(--text-secondary);
  }

  .header:not(.header--transparent) .nav-link {
    color: var(--text-heading);
  }

  .header:not(.header--transparent) .nav-link:hover {
    color: var(--color-accent-600);
  }

  .header:not(.header--transparent) .nav-link::after {
    background: var(--color-accent-600);
  }

  .header:not(.header--transparent) .mobile-menu-toggle {
    color: var(--text-heading);
  }

  /* ============================================
     LOGO — enlarged, responsive
     ============================================ */
  .navbar-logo {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    transition: color 0.35s ease;
  }

  .navbar-logo:hover {
    opacity: 0.9;
  }

  .logo-icon {
    transition: color 0.35s ease;
    flex-shrink: 0;
  }

  .logo-text {
    display: flex;
    flex-direction: column;
    line-height: 1.2;
  }

  .logo-name {
    font-family: var(--font-heading);
    font-size: clamp(1.3rem, 2.5vw, 1.85rem);
    font-weight: 700;
    transition: color 0.35s ease;
  }

  .logo-tagline {
    font-size: clamp(0.75rem, 1.5vw, 1rem);
    font-weight: 600;
    transition: color 0.35s ease;
  }

  /* ============================================
     DESKTOP NAV — with underline hover animation
     ============================================ */
  .nav-menu {
    display: flex;
    align-items: center;
    gap: 0.25rem;
    list-style: none;
    margin: 0;
    padding: 0;
  }

  .nav-item {
    position: relative;
  }

  .nav-link {
    display: flex;
    align-items: center;
    gap: 0.25rem;
    padding: 0.5rem 1rem;
    font-weight: 500;
    position: relative;
    transition: color 0.3s ease;
  }

  /* Underline animation on hover */
  .nav-link::after {
    content: "";
    position: absolute;
    bottom: 2px;
    left: 1rem;
    right: 1rem;
    height: 2px;
    border-radius: 1px;
    transform: scaleX(0);
    transform-origin: center;
    transition: transform 0.3s ease;
    will-change: transform;
  }

  .nav-link:hover::after {
    transform: scaleX(1);
  }

  .dropdown-icon {
    transition: transform var(--transition-fast);
  }

  .has-dropdown:hover .dropdown-icon {
    transform: rotate(180deg);
  }

  .dropdown-menu {
    position: absolute;
    top: 100%;
    left: 0;
    min-width: 200px;
    background: var(--surface-primary);
    border-radius: 0.5rem;
    box-shadow: var(--shadow-lg);
    list-style: none;
    padding: 0.5rem 0;
    margin: 0;
    opacity: 0;
    visibility: hidden;
    transform: translateY(10px);
    transition: all var(--transition-fast);
  }

  .has-dropdown:hover .dropdown-menu {
    opacity: 1;
    visibility: visible;
    transform: translateY(0);
  }

  .dropdown-link {
    display: block;
    padding: 0.75rem 1.25rem;
    color: var(--text-secondary);
    transition: all var(--transition-fast);
  }

  .dropdown-link:hover {
    background: var(--surface-secondary);
    color: var(--text-heading);
  }

  /* ============================================
     MOBILE MENU
     ============================================ */
  .mobile-menu-toggle {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 44px;
    height: 44px;
    background: transparent;
    border: none;
    cursor: pointer;
    transition: color 0.35s ease;
  }

  .mobile-menu-toggle .close-icon {
    display: none;
  }

  .mobile-menu-toggle.active .menu-icon {
    display: none;
  }

  .mobile-menu-toggle.active .close-icon {
    display: block;
  }

  .mobile-nav {
    position: absolute;
    top: 100%;
    left: 0;
    right: 0;
    background: var(--surface-primary);
    border-top: 1px solid var(--border-color);
    box-shadow: var(--shadow-lg);
    max-height: 0;
    overflow: hidden;
    transition: max-height 0.3s ease;
  }

  .mobile-nav.active {
    max-height: 80vh;
    overflow-y: auto;
  }

  .mobile-nav-menu {
    list-style: none;
    padding: 1rem 0;
    margin: 0;
  }

  .mobile-nav-link {
    display: block;
    padding: 1rem 0;
    color: var(--text-body);
    font-size: 1.125rem;
    font-weight: 500;
    border-bottom: 1px solid var(--border-light);
  }

  .mobile-dropdown {
    list-style: none;
    padding: 0;
    margin: 0;
    padding-left: 1rem;
  }

  .mobile-dropdown-link {
    display: block;
    padding: 0.75rem 0;
    color: var(--text-secondary);
    font-size: 1rem;
  }

  .mobile-cta {
    width: 100%;
    margin: 1rem 0 2rem;
  }

  /* Show sun in dark mode, moon in light mode */
  :global([data-theme="dark"]) .theme-icon-moon { display: none; }
  :global([data-theme="light"]) .theme-icon-sun { display: none; }

  /* Mobile theme toggle */
  .theme-toggle-mobile {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.75rem;
    width: 100%;
    padding: 0.875rem;
    background: var(--surface-secondary);
    border: 1px solid var(--border-color);
    border-radius: 0.75rem;
    cursor: pointer;
    color: var(--text-body);
    font-size: 0.95rem;
    font-weight: 600;
    margin: 0.75rem 0;
    transition: all 0.2s ease;
  }

  .theme-toggle-mobile:hover {
    border-color: var(--color-accent-500);
  }

  /* ============================================
     RESPONSIVE
     ============================================ */
  @media (max-width: 768px) {
    .header-top-text {
      font-size: 0.75rem;
    }

    .logo-name {
      font-size: 1.2rem;
    }

    .logo-tagline {
      font-size: 1rem;
    }
  }
</style>

<script>
  // Mobile menu toggle
  document.addEventListener("DOMContentLoaded", () => {
    const toggle = document.querySelector(".mobile-menu-toggle");
    const mobileNav = document.querySelector(".mobile-nav");

    if (toggle && mobileNav) {
      toggle.addEventListener("click", () => {
        toggle.classList.toggle("active");
        mobileNav.classList.toggle("active");
        const isExpanded = toggle.classList.contains("active");
        toggle.setAttribute("aria-expanded", String(isExpanded));
      });

      // Close menu when clicking a link
      mobileNav.querySelectorAll("a").forEach((link) => {
        link.addEventListener("click", () => {
          toggle.classList.remove("active");
          mobileNav.classList.remove("active");
          toggle.setAttribute("aria-expanded", "false");
        });
      });
    }

    // ====================================
    // Navbar transparent → solid on scroll
    // ====================================
    const header = document.getElementById("siteHeader");
    const heroSection = document.querySelector("main.has-hero section:first-of-type, #site-hero, [id^='hero-']");

    if (header && heroSection) {
      const isLightHero = heroSection.id.includes('-light') || heroSection.className.includes('L');
      if (isLightHero) {
        header.classList.add('header--light-bg');
      }

      const observer = new IntersectionObserver(
        (entries) => {
          entries.forEach((entry) => {
            if (entry.isIntersecting) {
              // Hero is visible → transparent navbar
              header.classList.add("header--transparent");
            } else {
              // Hero is out of view → solid navbar
              header.classList.remove("header--transparent");
            }
          });
        },
        {
          threshold: 0.05,
          rootMargin: "-80px 0px 0px 0px",
        },
      );

      observer.observe(heroSection);
    } else if (header) {
      // No hero section on this page → always solid
      header.classList.remove("header--transparent");
    }

    // ====================================
    // Theme toggle
    // ====================================
    const themeToggle = document.getElementById('themeToggle');
    const themeToggleMobile = document.getElementById('themeToggleMobile');

    function toggleTheme() {
      const html = document.documentElement;
      const current = html.getAttribute('data-theme');
      const next = current === 'dark' ? 'light' : 'dark';
      html.setAttribute('data-theme', next);
      localStorage.setItem('theme', next);

      // Update header light-bg class for light hero detection
      const heroEl = document.querySelector("main.has-hero section:first-of-type, #site-hero, [id^='hero-']");
      if (heroEl && header) {
        const isLightHero = next === 'light';
        header.classList.toggle('header--light-bg', isLightHero);
      }
    }

    if (themeToggle) themeToggle.addEventListener('click', toggleTheme);
    if (themeToggleMobile) themeToggleMobile.addEventListener('click', toggleTheme);
  });
</script>
